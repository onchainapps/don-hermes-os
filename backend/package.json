{
  "name": "don-os-backend",
  "version": "1.0.0",
  "description": "Standalone Bun backend server with Hermes integration",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun run --watch server.ts",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "node-pty": "^1.1.0",
    "ws": "^8.20.0",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.2"
  }
}
